'use strict';
/**
 * On-demand lookup of official TAMU catalog course titles (proper Title Case).
 * Removed in this public repo — see README for why.
 */

async function getCourseCatalogTitle() {
  return null;
}

module.exports = { getCourseCatalogTitle };
