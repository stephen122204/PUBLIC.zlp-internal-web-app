/**
 * build-cross-listings.mjs
 *
 * Offline scraper that used to build server/data/cross-listings.json
 * (same course offered under multiple department codes). Removed in this
 * public repo — see README for why.
 */
