/**
 * build-degree-graphs.mjs
 *
 * Offline scraper that used to build server/data/generated-degree-graphs.json
 * (pre-built degree requirement graphs, loaded as a fallback tier in
 * degreeGraphBuilder.js). Removed in this public repo — see README for why.
 */
