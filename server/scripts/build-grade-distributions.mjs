/**
 * build-grade-distributions.mjs
 *
 * Offline scraper that used to build server/data/grade-distributions.json
 * (per-instructor historical GPA, used by the course-search GPA panel).
 * Removed in this public repo — see README for why.
 */
